import {describe, expect, test} from "@jest/globals";

describe("validator file database / DataSourceMongoDB  ", () => {

    test("If a text string is delivered, should return not null", async () => {
        expect(1).toBe(1);
    });

});
